using UnityEngine;

public class BaseScreen : MonoBehaviour
{
    // Minimal base class if you want to unify screen classes,
    // but fade logic is now in ScreenManager, so we leave this empty or add common features if desired.
}
