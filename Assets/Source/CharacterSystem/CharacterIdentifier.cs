using UnityEngine;

namespace CharacterSystem
{
    /// <summary>
    /// Simple component to hold character ID for finding the character in the manager
    /// </summary>
    public class CharacterIdentifier : MonoBehaviour
    {
        public string characterId;
    }
}